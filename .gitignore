node_modules/
dist/
.vite/
*.log

# Compiler output. Regenerated by `npm run compact` locally (--skip-zk) or by
# the proving-keys workflow (with keys). Not committed: the prover keys are
# large binaries and everything here is reproducible from the .compact source.
managed/

# Proving keys copied into the UI for FetchZkConfigProvider to serve.
ui/public/keys/
ui/public/zkir/
